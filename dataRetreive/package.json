{
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "mkdirp": "^0.5.1",
    "jsdom": "^11.1.0"
  },
  "scripts": {
    "dev": "babel src -d dist && node dist/index.js",
    "loader": "babel src -d dist && node dist/dataLoader.js"
  }
}
